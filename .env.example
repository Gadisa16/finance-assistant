# Copy this file to `.env` and adjust values for your environment.
# IMPORTANT: Never commit real secrets (LLM_API_KEY, database passwords) to source control.

# ---------------------------------------------------------------------------
# Database connection string (SQLAlchemy URL).
# Default points to the Postgres service defined in docker-compose.
DATABASE_URL=postgresql+psycopg2://postgres:postgres@db:5432/finance

# ---------------------------------------------------------------------------
# LLM configuration.
# Set stub://local to use the built‑in deterministic fallback (no external calls).
# To enable Groq, REPLACE the below values as follows (example):
#   LLM_API_URL=https://api.groq.com/openai/v1
#   LLM_API_KEY=YOUR_GROQ_KEY   (keep secret!)
#   LLM_MODEL=llama-3.3-70b-versatile
# You can also experiment with other Groq models; keep temperature low for factual finance answers.
LLM_API_URL=stub://local
LLM_API_KEY=change-me
LLM_MODEL=llama-3.3-70b-versatile

# ---------------------------------------------------------------------------
# Directory inside the backend container where uploaded files are stored.
UPLOAD_DIR=/data/uploads

# ---------------------------------------------------------------------------
# Environment flag (e.g., dev / prod); currently used for informational purposes.
ENV=dev

# ---------------------------------------------------------------------------
# Additional optional settings (uncomment as needed):
# LOG_LEVEL=info
# MAX_UPLOAD_MB=25
# REQUEST_TIMEOUT_SECONDS=60
# ---------------------------------------------------------------------------
