# Backend local development environment example
# Copy to backend/.env for local runs (NOT required when using docker-compose; compose reads root .env).
# Do NOT commit real secrets.

DATABASE_URL=postgresql+psycopg2://postgres:postgres@db:5432/finance
LLM_API_URL=https://api.groq.com/openai/v1
LLM_API_KEY=YOUR_GROQ_KEY
LLM_MODEL=llama-3.3-70b-versatile
UPLOAD_DIR=/data/uploads
ENV=dev
