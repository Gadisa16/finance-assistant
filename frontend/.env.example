# Frontend environment variables example
# Copy to .env and adjust as needed.
# When running via Docker Compose with service name 'backend', this default works.
VITE_API_URL=http://localhost:8000

# If using Docker network and backend container hostname is 'backend':
# VITE_API_URL=http://backend:8000

# IMPORTANT: Do NOT put LLM secrets (LLM_API_KEY) in the frontend .env.
# Place LLM_API_URL/LLM_API_KEY/LLM_MODEL in the ROOT .env (next to docker-compose.yml)
# or in backend environment when running the API locally.